import SudokuMatrix from './SudokuMatrix';

const solveSudoku = (matrix: Number[][]): Number[][] => {

    for (let i = 0; i < matrix.length; i++) {
        for (let x = 0; x < matrix[i].length; x++) {
            //solveCell();
        }
    }

    return matrix;

}

function solveCell() {
    
}

export default solveSudoku;


